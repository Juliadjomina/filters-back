server:
  port: 8080
  servlet:
    context-path: /api
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/filters_test
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: mysecretpassword
  flyway:
    enabled: true
    locations: classpath:testdb/db_init

